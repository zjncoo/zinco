function mode() {
    let body = document.body;
    body.classList.toggle("dark-mode"); //Quando viene eseguita la funzione viene aggiunta la classe "dark-mode" al body.
}

function acquista(){
    alert("Articolo aggiunto al carrello."); //Quando viene cliccato il bottone "ACQUISTA" viene visualizzato un alert.
}


